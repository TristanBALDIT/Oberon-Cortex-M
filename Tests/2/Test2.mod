MODULE Test2; 

VAR i, j, k, n: INTEGER;
    x, y: REAL;
    s, t, u: SET;
    
BEGIN
    i := (i + 3) * (i - 5);
    j := i;

    k := k DIV 17;
    k := 8 * n;
    k := n DIV 2;
    k := n MOD 16;

    x := y;
    x := -y / (x - 1.1);

    s := t;
    s := s + t * u
END Test2.